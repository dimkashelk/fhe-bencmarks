# 📁 Структура проекта

```
bench/
├── Makefile              # Автоматизация сборки и запуска
├── QUICKSTART.md         # Быстрый старт (2 минуты чтения)
├── README.md             # Полная документация
├── STRUCTURE.md          # Этот файл - описание структуры
├── main.go               # Основной код бенчмарков
├── helpers.go            # Вспомогательные функции (сжатие)
├── go.mod                # Зависимости Go модуля
├── go.sum                # Контрольные суммы зависимостей
├── lattigo-bench         # Собранный бинарник (после make build)
└── results.csv           # Результаты бенчмарков (после make run)
```

## Описание файлов

### 📄 main.go (13 KB)
Основной файл с бенчмарками:
- Определение операций и параметров
- Функции `benchBGV()` и `benchCKKS()` для каждой схемы
- Генерация CSV с результатами
- **100 итераций** для каждой операции

### 🔧 helpers.go (507 B)
Вспомогательные функции:
- `compressZLIB()` - сжатие ZLIB
- `compressZstd()` - сжатие Zstandard

### ⚙️ Makefile (4.8 KB)
Автоматизация всего процесса:
- `make all` - полный цикл (deps → build → run)
- `make help` - справка по командам
- `make info` - информация о проекте
- `make view` - показать результаты
- `make clean` - очистка

### 📖 Документация

**QUICKSTART.md** (1.9 KB)
- Для тех, кто хочет быстро начать
- Минимум текста, максимум действий

**README.md** (6.3 KB)
- Полное описание проекта
- Детали реализации
- Примечания по схемам

**STRUCTURE.md** (этот файл)
- Структура проекта
- Описание каждого файла

## Измеряемые операции (11 шт.)

1. ⚙️ Генерация релинеаризации
2. ⚙️ Генерация Galois ключей
3. 🔒 Шифрование
4. 🔓 Дешифрование
5. ➕ Сложение
6. ✖️ Умножение
7. 🔄 Релионеаризация
8. 🔁 Вращение (rotate rows 1 шаг)
9. 💾 Сериализация шифротекста
10. 📦 Сжатая сериализация (ZLIB)
11. 📦 Сжатая сериализация (Zstandard)

## Схемы и параметры (9 конфигураций)

**Схемы:**
- **BFV** (через BGV в Lattigo v6)
- **CKKS** (комплексные числа)
- **BGV** (целочисленная)

**Размеры колец:**
- **4096** (LogN=12) - быстро, ~1-2 сек на схему
- **8192** (LogN=13) - средне, ~5-10 сек на схему
- **16384** (LogN=14) - медленно, ~30-60 сек на схему

## Зависимости

```go
github.com/tuneinsight/lattigo/v6  // Основная библиотека HE
github.com/klauspost/compress/zstd // Zstandard сжатие
```

## Выходной формат (results.csv)

```csv
Операция,BFV 4096,BFV 8192,...
Генерация релинеаризации,633.580,3957.370,...
...
```

**Единицы измерения:** микросекунды (μs)

**Формат чисел:** с точностью до 3 знаков после запятой

## Производительность

**Типичное время выполнения:**
- Полный бенчмарк: **2-5 минут**
- Одна схема (4096): **~2 секунды**
- Одна схема (8192): **~10 секунд**
- Одна схема (16384): **~40 секунд**

**Требования:**
- RAM: 4-8 GB (для 16384)
- CPU: любой современный (результаты зависят от процессора)
- Диск: ~15 MB для бинарника

## Как изменить параметры

**Изменить количество итераций:**
```go
// В main.go, функции benchBGV и benchCKKS
N := 100  // Измените на нужное значение
```

**Добавить новые размеры колец:**
```go
// В main.go
var degrees = []int{4096, 8192, 16384, 32768}  // Добавить 32768
```

**Добавить новые операции:**
1. Добавить в массив `ops` в `main.go`
2. Добавить измерение в функции `benchBGV()` / `benchCKKS()`
3. Пересобрать: `make build`

## Рекомендации для пользователей

**Первый запуск:**
```bash
make info  # Сначала прочитайте что будет происходить
make all   # Затем запустите всё
```

**Быстрое тестирование (малые параметры):**
Измените в `main.go`:
```go
var degrees = []int{4096}  // Только самый быстрый размер
N := 20                     // Меньше итераций
```

**Для публикации результатов:**
```go
N := 1000  // Больше итераций = стабильнее результаты
```

## Устранение проблем

**Ошибка "cannot find package":**
```bash
make clean-all
make all
```

**"Out of memory":**
- Уменьшите N
- Закройте другие программы
- Запускайте только 4096 и 8192

**Долго выполняется:**
- Это нормально для 16384
- Можно прервать Ctrl+C и использовать `make view`
